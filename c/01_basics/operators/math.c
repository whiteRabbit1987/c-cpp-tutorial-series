#include <stdio.h>
#include <math.h>

int main(){
    
    // int x = -9;
    // x = sqrt(x);
    // x = pow(x, 2);
    // x = abs(x);
    // printf("%d\n", x);

    float y = 3.94;
    // y = round(y);
    // y = ceil(y);
    // y = floor(y);
    // y = log(y);
    // y = sin(y);
    // y = cos(y);
    // y = tan(y);

    printf("%f\n", y);
    
    return 0;
}
























